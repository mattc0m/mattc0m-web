'use client';
'use strict';

var React = require('react');
var core = require('@mantine/core');
var Spotlight_module = require('./Spotlight.module.css.cjs');
var Spotlight_context = require('./Spotlight.context.cjs');

const defaultProps = {};
const SpotlightFooter = core.factory((props, ref) => {
  const { className, style, classNames, styles, ...others } = core.useProps(
    "SpotlightFooter",
    defaultProps,
    props
  );
  const ctx = Spotlight_context.useSpotlightContext();
  return /* @__PURE__ */ React.createElement(
    core.Box,
    {
      ref,
      ...ctx.getStyles("footer", { className, classNames, style, styles }),
      ...others
    }
  );
});
SpotlightFooter.classes = Spotlight_module;
SpotlightFooter.displayName = "@mantine/spotlight/SpotlightFooter";

exports.SpotlightFooter = SpotlightFooter;
//# sourceMappingURL=SpotlightFooter.cjs.map
